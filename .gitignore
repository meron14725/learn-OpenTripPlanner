# TrainGTFSGenerator is a separate git repository (https://github.com/fksms/TrainGTFSGenerator)
# Managed independently; the generated GTFS files are committed to data/GTFS-data/
TrainGTFSGenerator/

# macOS duplicate files
*\ 2.zip
.DS_Store

# TrainGTFSGenerator source JSON data (stations, timetables, etc.)
data/json-data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.env

# OTP2 large binary files (generated / downloaded)
data/*.pbf
data/*.obj
otp.jar

# GTFS-data backup directory (moved from data/GTFS-data to project root)
GTFS-data/
data/osm/*.osm.pbf

# OTP2 build directories (symlink scaffolding, machine-specific)
data/build-baseline/
data/build-4pref/

# OTP2 build logs (large, generated)
logs/
